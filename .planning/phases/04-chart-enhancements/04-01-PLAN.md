---
phase: 04-chart-enhancements
plan: 01
type: execute
wave: 1
depends_on: [03]
files_modified:
  - app/dashboard/_components/ChartBlock.tsx
  - app/dashboard/_components/ChartBlock.test.tsx
  - app/dashboard/_components/ExportButton.tsx
  - app/dashboard/_components/MarkdownMessage.tsx
autonomous: true
requirements: [CHART-05, CHART-06, EXPORT-04]
must_haves:
  truths:
    - Users can switch between bar, line, and pie charts for the same data without new AI calls.
    - Each chart displays an AI-generated insight caption below the visualization.
    - Users can copy chart images and text answers to clipboard with one click.
  artifacts:
    - path: "app/dashboard/_components/ChartBlock.tsx"
      provides: "Chart rendering and type switching UI"
    - path: "app/dashboard/_components/ChartBlock.test.tsx"
      provides: "Tests for chart switching, captions, and copy"
    - path: "app/dashboard/_components/ExportButton.tsx"
      provides: "Clipboard copy for text answers"
    - path: "app/dashboard/_components/MarkdownMessage.tsx"
      provides: "Clipboard copy for code/text blocks"
  key_links:
    - from: "ChartBlock.tsx"
      to: "ChartBlock.test.tsx"
      via: "UI and behavior tests"
    - from: "ChartBlock.tsx"
      to: "ExportButton.tsx"
      via: "Clipboard copy logic"
---

<objective>
Enhance the charting experience by enabling users to switch chart types (bar/line/pie) for the same data without new AI calls, display AI-generated insight captions below charts, and provide one-click copy for chart images and text answers.
Purpose: Improves usability, insight clarity, and sharing.
Output: Updated chart component, UI controls, caption rendering, and copy-to-clipboard features.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@/app/dashboard/_components/ChartBlock.tsx
@/app/dashboard/_components/ExportButton.tsx
@/app/dashboard/_components/MarkdownMessage.tsx
</execution_context>

<context>
- ChartBlock.tsx renders all chart types using recharts.
- No existing chart type switcher UI.
- ExportButton and MarkdownMessage already implement text copy to clipboard.
- No insight/caption rendering below charts yet.
</context>

<interfaces>
From app/dashboard/_components/ChartBlock.tsx:
```typescript
export interface ChartBlockProps {
  spec: {
    x: string;
    y: string;
    type: "bar" | "line" | "pie" | "scatter";
  };
  data: Record<string, unknown>[];
}
export function ChartBlock({ spec, data }: ChartBlockProps)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add chart type switcher UI to ChartBlock</name>
  <files>app/dashboard/_components/ChartBlock.tsx, app/dashboard/_components/ChartBlock.test.tsx</files>
  <action>
    - Add a UI control (e.g., segmented button or dropdown) above the chart in ChartBlock.tsx to let users switch between "bar", "line", and "pie" types.
    - When the user switches type, update the rendered chart without making a new AI call or changing the data prop.
    - Ensure the switcher is accessible and styled with shadcn/ui and Tailwind.
    - Update ChartBlock.test.tsx to cover chart switching and correct rendering.
  </action>
  <verify>
    <automated>pnpm test -r --filter ChartBlock</automated>
  </verify>
  <done>
    Users can switch chart types for the same data instantly; tests verify correct chart rendering for each type.
  </done>
</task>

<task type="auto">
  <name>Task 2: Display AI-generated insight captions below charts</name>
  <files>app/dashboard/_components/ChartBlock.tsx, app/dashboard/_components/ChartBlock.test.tsx</files>
  <action>
    - Extend ChartBlockProps to accept an optional `caption` or `insight` string.
    - Render the caption below the chart visualization, styled as a secondary text block.
    - Ensure the caption is visible for all chart types and does not overlap with chart controls.
    - Add tests to ChartBlock.test.tsx to verify caption rendering.
  </action>
  <verify>
    <automated>pnpm test -r --filter ChartBlock</automated>
  </verify>
  <done>
    Insight captions appear below charts when provided; tests confirm correct display.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add one-click copy for chart images and text answers</name>
  <files>app/dashboard/_components/ChartBlock.tsx, app/dashboard/_components/ExportButton.tsx, app/dashboard/_components/MarkdownMessage.tsx, app/dashboard/_components/ChartBlock.test.tsx</files>
  <action>
    - Add a "Copy chart image" button to ChartBlock.tsx that copies the rendered chart as an image (use HTMLCanvas or SVG-to-clipboard as appropriate).
    - Ensure the button is accessible, uses shadcn/ui, and provides user feedback ("Copied!").
    - In ExportButton.tsx and MarkdownMessage.tsx, ensure copy-to-clipboard for text answers is available and consistent with the new chart copy UX.
    - Add/extend tests to verify copy functionality for both chart images and text.
  </action>
  <verify>
    <automated>pnpm test -r --filter ChartBlock</automated>
  </verify>
  <done>
    Users can copy chart images and text answers with one click; tests confirm clipboard contents.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI → Clipboard | User-initiated copy actions cross browser trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | ChartBlock copy button | mitigate | Button only appears for rendered charts, uses browser clipboard API |
| T-04-02 | Tampering | Clipboard contents | mitigate | Only user-initiated copy allowed, no background clipboard writes |
| T-04-03 | Information Disclosure | Caption rendering | accept | Captions are user-visible, no sensitive data |
</threat_model>

<verification>
- All tests in ChartBlock.test.tsx pass for chart switching, caption, and copy features.
- Manual check: User can switch chart types, see captions, and copy chart/text in the dashboard UI.
</verification>

<success_criteria>
- Users can switch between bar, line, and pie charts for the same data without new AI calls.
- Insight captions appear below charts.
- One-click copy for chart images and text answers works and provides feedback.
</success_criteria>

<output>
After completion, create .planning/phases/04-chart-enhancements/04-01-SUMMARY.md
</output>
